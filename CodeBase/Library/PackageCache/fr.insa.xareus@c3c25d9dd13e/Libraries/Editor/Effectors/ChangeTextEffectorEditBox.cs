namespace Xareus.Unity.Editor.Scenarios.UI
{
    public class ChangeTextEffectorEditBox
    {
        // TODO
        // Merge with the TMP one with a choice?

        // One CustomEditBox per mode?
        // automaticaly apply a [ProvideConstraint(typeof(Component))] with multiple types to constraint the type selector?
        // custom dropdown with the types and display a typed field?

        // Same problem as SetAnimatorParameterValueEffector
    }
}
